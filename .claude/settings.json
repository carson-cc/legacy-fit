{
  "defaultShell": "bash",
  "hooks": {
    "WorktreeCreate": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); branch=$(echo \"$input\" | jq -r '.branch // \"worktree-\" + (.worktree_id // \"tmp\")'); path=$(echo \"$input\" | jq -r '.path'); git -C \"/c/Users/carso/OneDrive/Desktop/legacy-fit\" worktree add \"$path\" -b \"$branch\" 2>/dev/null || git -C \"/c/Users/carso/OneDrive/Desktop/legacy-fit\" worktree add \"$path\" 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ],
    "WorktreeRemove": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "input=$(cat); path=$(echo \"$input\" | jq -r '.path'); git -C \"/c/Users/carso/OneDrive/Desktop/legacy-fit\" worktree remove \"$path\" --force 2>/dev/null || true",
            "timeout": 30
          }
        ]
      }
    ]
  }
}
